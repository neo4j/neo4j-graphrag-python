================================================================================
TASK-001: Add Neo4j server version detection utility with caching
Date: 2026-03-13T14:00:00Z
================================================================================

Summary:
Added get_version_cached(), clear_version_cache(), and supports_search_clause()
to version_utils.py. Uses WeakKeyDictionary for per-driver caching so entries
auto-clean on driver GC. supports_search_clause() catches all exceptions and
returns False on error for graceful fallback.

Files Changed:
- src/neo4j_graphrag/utils/version_utils.py (modified: added caching + SEARCH clause check)
- tests/unit/utils/test_version_utils.py (modified: added 11 new tests for caching and SEARCH support)

Key Decisions:
- WeakKeyDictionary keyed on driver instance for cache — no manual cleanup needed
- supports_search_clause catches broad Exception to handle any driver/network error
- Version threshold is (2026, 1, 0) — tuple comparison works naturally with year-based versioning

Next Task: task-002

================================================================================
TASK-002: Add filter classification for SEARCH-compatible predicates
Date: 2026-03-13T14:30:00Z
================================================================================

Summary:
Added classify_filter_for_search() and helper _is_filter_search_compatible()
to filters.py. Returns FilterClassification dataclass with is_compatible flag,
optional Cypher WHERE clause, and params. Compatible = simple comparisons
(=, <>, <, <=, >, >=, BETWEEN) joined by AND only. Incompatible = $or, $in,
$nin, $like, $ilike, nested OR logic.

Files Changed:
- src/neo4j_graphrag/filters.py (added FilterClassification, classify_filter_for_search, _is_filter_search_compatible, SEARCH_COMPATIBLE_OPERATORS)
- tests/unit/test_filters.py (added 20 tests covering all operator types and edge cases)

Key Decisions:
- Reuses existing get_metadata_filter() for Cypher generation when compatible
- FilterClassification is a dataclass, not a NamedTuple, for clarity
- SEARCH_COMPATIBLE_OPERATORS set defined separately for maintainability
- None/empty filters classified as compatible with no WHERE clause

Next Task: task-003

================================================================================
TASK-003: Add SEARCH clause Cypher query builder in neo4j_queries.py
Date: 2026-03-13T15:00:00Z
================================================================================

Summary:
Added _build_search_clause_vector_query() to neo4j_queries.py. Generates
MATCH (node:Label) SEARCH node IN (VECTOR INDEX indexName FOR $query_vector
[WHERE ...] LIMIT $top_k) SCORE AS score. Accepts optional FilterClassification
to embed WHERE predicates inside SEARCH clause for in-index filtering.

Files Changed:
- src/neo4j_graphrag/neo4j_queries.py (added _build_search_clause_vector_query, imported FilterClassification)
- tests/unit/test_neo4j_queries.py (added 8 tests in TestBuildSearchClauseVectorQuery class)

Key Decisions:
- Uses node alias "node" to match existing query builder conventions
- Backtick-escapes index name and node label for safety
- Uses $query_vector and $top_k params matching existing parameter naming
- Filter params merged into return dict when FilterClassification provided
- Function is private (_build) since it'll be called internally by retrievers

Next Task: task-004

================================================================================
TASK-004: Add filterable_properties support to create_vector_index
Date: 2026-03-13T15:30:00Z
================================================================================

Summary:
Extended create_vector_index() with optional filterable_properties: List[str]
parameter. When provided and non-empty, appends WITH [n.`prop1`, n.`prop2`]
clause to the CREATE VECTOR INDEX Cypher. Property names are backtick-escaped.
Empty list or None produces unchanged Cypher (backward compatible).

Files Changed:
- src/neo4j_graphrag/indexes.py (added filterable_properties param + WITH clause generation)
- tests/unit/test_indexes.py (added 4 tests: multi-prop, single-prop, empty list, None)

Key Decisions:
- Parameter placed after neo4j_database to avoid breaking positional callers
- Backtick-escapes property names in WITH clause for safety
- Empty list treated same as None (no WITH clause) via truthiness check
- All 25 index tests pass, mypy strict clean, ruff clean

Next Task: task-005

================================================================================
TASK-005: Integrate SEARCH clause into VectorRetriever and VectorCypherRetriever
Date: 2026-03-13T16:00:00Z
================================================================================

Summary:
Modified VectorRetriever and VectorCypherRetriever get_search_results() to
detect SEARCH clause support via supports_search_clause() and route queries
accordingly. On Neo4j 2026.01+ with compatible filters (or no filters) and
known node label, uses _build_search_clause_vector_query() + get_query_tail().
Otherwise falls back to existing get_search_query() procedure path. Logs
warning when filters are incompatible on a SEARCH-capable server.

Files Changed:
- src/neo4j_graphrag/retrievers/vector.py (added routing logic, new imports)
- tests/unit/retrievers/test_vector.py (patched supports_search_clause=False on existing tests)

Key Decisions:
- supports_search_clause called at search time, not init — version may change between init and search
- FilterClassification computed once and reused (no redundant classify call)
- Existing tests patched with supports_search_clause=False to preserve old-path assertions
- SEARCH clause path only activates when _node_label is available (from _fetch_index_infos)
- VectorCypherRetriever passes retrieval_query to get_query_tail for SEARCH path

Next Task: task-006

================================================================================
TASK-006: Integrate SEARCH clause into HybridRetriever and HybridCypherRetriever
Date: 2026-03-13T17:00:00Z
================================================================================

Summary:
Extended HybridRetriever and HybridCypherRetriever with SEARCH clause routing.
Added _build_search_clause_fulltext_query(), _build_hybrid_search_clause_query(),
and _build_hybrid_search_clause_query_linear() to neo4j_queries.py. Both hybrid
retrievers now use SEARCH clause for both vector AND fulltext components on
Neo4j 2026.01+ with known node label, using CALL () { ... UNION ... } pattern.
Falls back to existing procedure path on older versions.

Files Changed:
- src/neo4j_graphrag/neo4j_queries.py (added 3 new query builders for fulltext SEARCH + hybrid SEARCH)
- src/neo4j_graphrag/retrievers/hybrid.py (added SEARCH clause routing, imports, _node_label init, _fetch_index_infos in HybridCypherRetriever)
- tests/unit/retrievers/test_hybrid.py (patched supports_search_clause=False + _fetch_index_infos on existing tests)

Key Decisions:
- SEARCH clause used for BOTH vector and fulltext (not just vector) since Neo4j 2026 supports FULLTEXT INDEX in SEARCH
- HybridCypherRetriever now calls _fetch_index_infos in __init__ (needed for _node_label for SEARCH routing)
- Both _node_label and _embedding_node_property initialized to None before _fetch_index_infos call
- No filter support in hybrid (same as before — hybrid already raises on filters)
- Uses CALL () { ... UNION ... } syntax (new 5.23+ syntax) since SEARCH requires 2026.01+
- Existing 22 hybrid tests pass with supports_search_clause=False patch

Next Task: task-007
